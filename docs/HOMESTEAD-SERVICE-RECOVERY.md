# Homestead — Service Recovery

NEEDS_HELP (or safety-classified negative) opens recovery on `revenue_jobs`:

OPEN → CONTACTED → RESOLVED

Telegram alert to operators with `recovery` delivery kind. Resolve schedules follow-up aftercare; does not auto-request review.

Open recovery blocks marketing retention (review reminder, maintenance blast, reactivation).
